#pragma once
#include <cstdint>

namespace TTT {

inline constexpr uint8_t COL = 3;  // Acceptable values: COL >= 3
inline constexpr uint8_t ROW = 3;  // Acceptable values: ROW >=3
inline constexpr uint8_t END_GAME_POSITION = -1;
inline constexpr char DEF_VAL = 'Q';

}  // namespace TTT